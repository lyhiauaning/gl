#if 0
	shc Version 4.0.3, Generic Shell Script Compiler
	GNU GPL Version 3 Md Jahidul Hamid <jahidulhamid@yahoo.com>

	shc -v -r -f code/gl.sh -o code-2/gl 
#endif

static  char data [] = 
#define      tst2_z	19
#define      tst2	((&data[1]))
	"\215\345\362\131\204\230\072\215\330\315\177\214\173\225\342\226"
	"\204\124\172\062\277"
#define      msg2_z	19
#define      msg2	((&data[24]))
	"\300\075\173\344\067\356\106\225\233\146\076\076\247\327\213\310"
	"\161\267\304\240\242\204"
#define      xecc_z	15
#define      xecc	((&data[46]))
	"\251\207\140\251\321\053\212\242\073\054\247\261\257\000\375\206"
	"\322\204"
#define      inlo_z	3
#define      inlo	((&data[61]))
	"\331\326\131"
#define      chk2_z	19
#define      chk2	((&data[67]))
	"\246\257\265\214\134\246\075\150\015\101\231\055\377\056\025\300"
	"\066\227\175\365\331\123\145\102\167"
#define      pswd_z	256
#define      pswd	((&data[115]))
	"\041\305\356\340\153\272\020\053\367\214\204\357\250\056\166\010"
	"\202\034\204\143\345\107\011\225\374\157\364\340\011\354\133\362"
	"\325\221\341\043\310\277\155\323\233\351\270\175\070\124\122\357"
	"\356\061\323\154\225\267\165\047\030\152\010\041\126\144\024\054"
	"\365\365\120\275\265\276\221\120\247\111\315\340\236\037\320\214"
	"\121\243\371\347\133\157\016\163\331\026\224\060\172\250\134\160"
	"\236\254\055\123\152\277\244\022\011\161\362\247\221\302\064\343"
	"\146\056\312\301\235\330\064\166\357\311\246\152\162\003\332\021"
	"\257\010\145\032\307\011\054\320\173\036\170\014\341\255\357\110"
	"\333\272\012\170\223\077\357\202\010\225\355\173\231\307\214\110"
	"\320\361\142\227\373\216\150\166\255\341\202\217\216\162\330\152"
	"\055\342\342\300\041\322\102\052\150\060\245\001\367\062\111\307"
	"\044\254\137\037\073\310\225\350\252\030\170\071\213\120\243\270"
	"\062\206\170\124\130\273\177\300\353\045\301\343\127\013\253\174"
	"\267\013\233\362\323\061\333\176\111\123\267\325\244\132\215\327"
	"\341\006\053\071\301\252\371\255\317\273\220\047\306\073\243\176"
	"\106\077\160\032\160\114\230\272\237\120\217\104\253\035\033\214"
	"\044\107\305\346\362\277\223\302\172\043\330\163\117\076\324\160"
	"\004\303\121\160\175\141\234\165\355\041\145\225\117\334\235\322"
	"\370\042"
#define      shll_z	10
#define      shll	((&data[397]))
	"\077\163\262\157\224\361\051\322\023\352\232\265\145"
#define      rlax_z	1
#define      rlax	((&data[408]))
	"\244"
#define      msg1_z	65
#define      msg1	((&data[418]))
	"\155\216\161\276\376\357\040\232\144\342\230\360\146\231\337\010"
	"\116\332\335\135\137\041\020\205\356\045\350\023\350\330\153\366"
	"\052\354\217\151\162\111\110\211\303\126\321\143\065\167\350\022"
	"\337\206\214\035\034\270\362\142\051\324\223\222\074\100\051\043"
	"\130\062\236\164\326\161\216\360\277\022\015\273\312\243\013\246"
	"\101\335\236\143\023"
#define      tst1_z	22
#define      tst1	((&data[498]))
	"\357\062\000\072\275\147\355\006\102\237\121\235\115\374\070\341"
	"\354\200\302\211\235\237\361\366\244\215\013"
#define      date_z	1
#define      date	((&data[521]))
	"\363"
#define      lsto_z	1
#define      lsto	((&data[522]))
	"\041"
#define      chk1_z	22
#define      chk1	((&data[524]))
	"\112\314\102\047\017\205\030\345\162\370\240\276\301\166\001\233"
	"\340\327\013\150\336\170\012"
#define      opts_z	1
#define      opts	((&data[546]))
	"\304"
#define      text_z	634
#define      text	((&data[592]))
	"\262\357\075\220\215\240\243\012\155\365\371\237\366\064\253\364"
	"\370\144\140\113\217\212\234\251\325\210\050\056\057\161\052\342"
	"\140\147\162\356\010\026\370\166\014\362\025\003\047\331\064\363"
	"\061\011\347\076\254\211\126\113\003\242\374\351\055\075\116\264"
	"\223\043\244\305\340\314\136\322\024\015\106\351\067\275\222\324"
	"\134\141\324\341\155\057\216\355\070\343\370\131\045\134\114\035"
	"\303\101\124\315\104\156\114\041\230\145\351\101\047\177\067\142"
	"\202\146\307\242\217\114\220\032\117\201\232\365\033\207\075\033"
	"\216\314\244\340\036\246\042\120\320\337\004\116\053\114\307\325"
	"\135\052\355\301\114\374\216\050\201\347\000\007\100\235\360\263"
	"\262\374\070\016\326\257\264\014\225\363\125\246\154\125\212\167"
	"\050\045\034\110\061\070\301\132\273\313\257\112\332\176\115\306"
	"\002\045\203\325\344\370\207\035\024\213\300\340\354\236\145\254"
	"\274\314\030\352\057\020\036\066\224\230\253\310\136\256\134\037"
	"\233\302\072\150\362\257\333\262\211\224\372\077\315\060\260\211"
	"\335\340\313\106\067\056\354\366\074\164\072\235\350\263\165\015"
	"\352\015\263\137\242\160\154\107\061\066\205\277\355\102\110\331"
	"\315\227\300\063\132\262\070\077\163\227\311\300\206\201\253\057"
	"\121\271\225\051\260\110\102\103\140\130\157\024\236\240\330\222"
	"\202\030\124\140\242\037\110\342\116\146\337\136\331\056\361\152"
	"\370\102\343\161\136\172\271\262\137\255\244\355\227\367\116\106"
	"\253\114\225\350\311\076\124\206\335\265\367\144\363\374\244\003"
	"\056\016\151\226\113\245\244\364\026\244\127\267\174\041\052\006"
	"\320\353\200\256\223\335\365\211\214\237\121\343\106\217\362\236"
	"\337\374\017\073\124\007\264\321\202\330\040\310\350\111\075\003"
	"\074\264\314\203\267\225\136\113\213\152\324\073\245\022\073\065"
	"\212\247\256\267\377\052\372\272\366\275\063\025\325\065\152\360"
	"\156\003\362\161\341\116\140\115\361\005\220\373\260\113\205\103"
	"\154\135\255\020\275\220\023\323\071\040\345\171\327\021\174\360"
	"\127\044\307\142\073\360\206\143\342\314\355\161\111\010\271\271"
	"\244\277\017\110\166\006\312\050\332\315\366\114\203\255\002\152"
	"\261\145\307\253\364\336\360\035\253\017\241\350\370\065\011\114"
	"\015\174\130\021\314\250\262\222\255\334\100\300\047\222\374\136"
	"\252\046\322\361\345\043\134\130\162\170\243\337\300\371\021\322"
	"\351\357\151\064\075\160\222\361\034\044\351\304\074\262\206\264"
	"\234\000\013\066\012\151\241\323\245\364\261\352\170\033\053\242"
	"\056\067\011\034\221\270\150\006\353\002\242\202\215\374\004\023"
	"\177\121\172\222\200\115\137\005\324\361\154\140\122\137\214\144"
	"\203\271\011\221\201\033\112\247\242\060\243\064\236\134\100\071"
	"\036\330\070\215\003\337\046\062\014\052\077\256\147\346\113\271"
	"\317\312\323\200\121\157\112\255\210\215\245\342\252\035\065\236"
	"\112\140\226\004\132\113\327\271\303\121\051\216\173\166\125\234"
	"\202\125\250\245\356\163\242\301\367\040\045\127\153\265\342\007"
	"\136\270\220\206\346\277\367\021\242\127\171\025\105\201\053\076"
	"\370\070\061\016\073\130\317\062\170\364\212\343\251\155\353\010"
	"\045\173\216\014\073\205\035\335\335\227\362\043\031\036\141\021"
	"\126\222\037\221\353\356\304\143\343\117\106\215\274\062\226\342"
	"\256\044\357\351\252\015\306\207\244\271\253\275\327\014\317\055"
	"\237\356\277\212\335\204\356\301\323\065\117\220\147\345\163\025"
	"\011\142\376\264\160\305\073\024\176\346\322\125\363\241\203\223"
	"\220\103\036\155\307\014\057\233\101\177\054\251\144\237\276\156"
	"\002\275\042\162\203\136\207\002\105\131\130\071\373\333\315\213"
	"\037\353\371"/* End of data[] */;
#define      hide_z	4096
#define SETUID 0	/* Define as 1 to call setuid(0) at start of script */
#define DEBUGEXEC	0	/* Define as 1 to debug execvp calls */
#define TRACEABLE	1	/* Define as 1 to enable ptrace the executable */
#define HARDENING	0	/* Define as 1 to disable ptrace/dump the executable */
#define BUSYBOXON	0	/* Define as 1 to enable work with busybox */

#if HARDENING
static const char * shc_x[] = {
"/*",
" * Copyright 2019 - Intika <intika@librefox.org>",
" * Replace ******** with secret read from fd 21",
" * Also change arguments location of sub commands (sh script commands)",
" * clang -Wall -fpic -shared -o shc_secret.so shc_secret.c -ldl",
" */",
"",
"#define _GNU_SOURCE /* needed to get RTLD_NEXT defined in dlfcn.h */",
"#define PLACEHOLDER \"********\"",
"#include <dlfcn.h>",
"#include <stdlib.h>",
"#include <string.h>",
"#include <unistd.h>",
"#include <stdio.h>",
"#include <signal.h>",
"",
"static char secret[128000]; //max size",
"typedef int (*pfi)(int, char **, char **);",
"static pfi real_main;",
"",
"// copy argv to new location",
"char **copyargs(int argc, char** argv){",
"    char **newargv = malloc((argc+1)*sizeof(*argv));",
"    char *from,*to;",
"    int i,len;",
"",
"    for(i = 0; i<argc; i++){",
"        from = argv[i];",
"        len = strlen(from)+1;",
"        to = malloc(len);",
"        memcpy(to,from,len);",
"        // zap old argv space",
"        memset(from,'\\0',len);",
"        newargv[i] = to;",
"        argv[i] = 0;",
"    }",
"    newargv[argc] = 0;",
"    return newargv;",
"}",
"",
"static int mymain(int argc, char** argv, char** env) {",
"    //fprintf(stderr, \"Inject main argc = %d\\n\", argc);",
"    return real_main(argc, copyargs(argc,argv), env);",
"}",
"",
"int __libc_start_main(int (*main) (int, char**, char**),",
"                      int argc,",
"                      char **argv,",
"                      void (*init) (void),",
"                      void (*fini)(void),",
"                      void (*rtld_fini)(void),",
"                      void (*stack_end)){",
"    static int (*real___libc_start_main)() = NULL;",
"    int n;",
"",
"    if (!real___libc_start_main) {",
"        real___libc_start_main = dlsym(RTLD_NEXT, \"__libc_start_main\");",
"        if (!real___libc_start_main) abort();",
"    }",
"",
"    n = read(21, secret, sizeof(secret));",
"    if (n > 0) {",
"      int i;",
"",
"    if (secret[n - 1] == '\\n') secret[--n] = '\\0';",
"    for (i = 1; i < argc; i++)",
"        if (strcmp(argv[i], PLACEHOLDER) == 0)",
"          argv[i] = secret;",
"    }",
"",
"    real_main = main;",
"",
"    return real___libc_start_main(mymain, argc, argv, init, fini, rtld_fini, stack_end);",
"}",
"",
0};
#endif /* HARDENING */

/* rtc.c */

#include <sys/stat.h>
#include <sys/types.h>

#include <errno.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <time.h>
#include <unistd.h>

/* 'Alleged RC4' */

static unsigned char stte[256], indx, jndx, kndx;

/*
 * Reset arc4 stte. 
 */
void stte_0(void)
{
	indx = jndx = kndx = 0;
	do {
		stte[indx] = indx;
	} while (++indx);
}

/*
 * Set key. Can be used more than once. 
 */
void key(void * str, int len)
{
	unsigned char tmp, * ptr = (unsigned char *)str;
	while (len > 0) {
		do {
			tmp = stte[indx];
			kndx += tmp;
			kndx += ptr[(int)indx % len];
			stte[indx] = stte[kndx];
			stte[kndx] = tmp;
		} while (++indx);
		ptr += 256;
		len -= 256;
	}
}

/*
 * Crypt data. 
 */
void arc4(void * str, int len)
{
	unsigned char tmp, * ptr = (unsigned char *)str;
	while (len > 0) {
		indx++;
		tmp = stte[indx];
		jndx += tmp;
		stte[indx] = stte[jndx];
		stte[jndx] = tmp;
		tmp += stte[indx];
		*ptr ^= stte[tmp];
		ptr++;
		len--;
	}
}

/* End of ARC4 */

#if HARDENING

#include <sys/ptrace.h>
#include <sys/wait.h>
#include <signal.h>
#include <sys/prctl.h>
#define PR_SET_PTRACER 0x59616d61

/* Seccomp Sandboxing Init */
#include <stdlib.h>
#include <stdio.h>
#include <stddef.h>
#include <string.h>
#include <unistd.h>
#include <errno.h>

#include <sys/types.h>
#include <sys/prctl.h>
#include <sys/syscall.h>
#include <sys/socket.h>

#include <linux/filter.h>
#include <linux/seccomp.h>
#include <linux/audit.h>

#define ArchField offsetof(struct seccomp_data, arch)

#define Allow(syscall) \
    BPF_JUMP(BPF_JMP+BPF_JEQ+BPF_K, SYS_##syscall, 0, 1), \
    BPF_STMT(BPF_RET+BPF_K, SECCOMP_RET_ALLOW)

struct sock_filter filter[] = {
    /* validate arch */
    BPF_STMT(BPF_LD+BPF_W+BPF_ABS, ArchField),
    BPF_JUMP( BPF_JMP+BPF_JEQ+BPF_K, AUDIT_ARCH_X86_64, 1, 0),
    BPF_STMT(BPF_RET+BPF_K, SECCOMP_RET_KILL),

    /* load syscall */
    BPF_STMT(BPF_LD+BPF_W+BPF_ABS, offsetof(struct seccomp_data, nr)),

    /* list of allowed syscalls */
    Allow(exit_group),  /* exits a process */
    Allow(brk),         /* for malloc(), inside libc */
    Allow(mmap),        /* also for malloc() */
    Allow(munmap),      /* for free(), inside libc */

    /* and if we don't match above, die */
    BPF_STMT(BPF_RET+BPF_K, SECCOMP_RET_KILL),
};
struct sock_fprog filterprog = {
    .len = sizeof(filter)/sizeof(filter[0]),
    .filter = filter
};

/* Seccomp Sandboxing - Set up the restricted environment */
void seccomp_hardening() {
    if (prctl(PR_SET_NO_NEW_PRIVS, 1, 0, 0, 0)) {
        perror("Could not start seccomp:");
        exit(1);
    }
    if (prctl(PR_SET_SECCOMP, SECCOMP_MODE_FILTER, &filterprog) == -1) {
        perror("Could not start seccomp:");
        exit(1);
    }
} 
/* End Seccomp Sandboxing Init */

void shc_x_file() {
    FILE *fp;
    int line = 0;

    if ((fp = fopen("/data/data/com.termux/files/usr/tmp/shc_x.c", "w")) == NULL ) {exit(1); exit(1);}
    for (line = 0; shc_x[line]; line++)	fprintf(fp, "%s\n", shc_x[line]);
    fflush(fp);fclose(fp);
}

int make() {
	char * cc, * cflags, * ldflags;
    char cmd[4096];

	cc = getenv("CC");
	if (!cc) cc = "clang";

	sprintf(cmd, "%s %s -o %s %s", cc, "-Wall -fpic -shared", "/data/data/com.termux/files/usr/tmp/shc_x.so", "/data/data/com.termux/files/usr/tmp/shc_x.c -ldl");
	if (system(cmd)) {remove("/data/data/com.termux/files/usr/tmp/shc_x.c"); return -1;}
	remove("/data/data/com.termux/files/usr/tmp/shc_x.c"); return 0;
}

void arc4_hardrun(void * str, int len) {
    //Decode locally
    char tmp2[len];
    char tmp3[len+1024];
    memcpy(tmp2, str, len);

	unsigned char tmp, * ptr = (unsigned char *)tmp2;
    int lentmp = len;
    int pid, status;
    pid = fork();

    shc_x_file();
    if (make()) {exit(1);}

    setenv("LD_PRELOAD","/data/data/com.termux/files/usr/tmp/shc_x.so",1);

    if(pid==0) {

        //Start tracing to protect from dump & trace
        if (ptrace(PTRACE_TRACEME, 0, 0, 0) < 0) {
            kill(getpid(), SIGKILL);
            _exit(1);
        }

        //Decode Bash
        while (len > 0) {
            indx++;
            tmp = stte[indx];
            jndx += tmp;
            stte[indx] = stte[jndx];
            stte[jndx] = tmp;
            tmp += stte[indx];
            *ptr ^= stte[tmp];
            ptr++;
            len--;
        }

        //Do the magic
        sprintf(tmp3, "%s %s", "'********' 21<<<", tmp2);

        //Exec bash script //fork execl with 'sh -c'
        system(tmp2);

        //Empty script variable
        memcpy(tmp2, str, lentmp);

        //Clean temp
        remove("/data/data/com.termux/files/usr/tmp/shc_x.so");

        //Sinal to detach ptrace
        ptrace(PTRACE_DETACH, 0, 0, 0);
        exit(0);
    }
    else {wait(&status);}

    /* Seccomp Sandboxing - Start */
    seccomp_hardening();

    exit(0);
}
#endif /* HARDENING */

/*
 * Key with file invariants. 
 */
int key_with_file(char * file)
{
	struct stat statf[1];
	struct stat control[1];

	if (stat(file, statf) < 0)
		return -1;

	/* Turn on stable fields */
	memset(control, 0, sizeof(control));
	control->st_ino = statf->st_ino;
	control->st_dev = statf->st_dev;
	control->st_rdev = statf->st_rdev;
	control->st_uid = statf->st_uid;
	control->st_gid = statf->st_gid;
	control->st_size = statf->st_size;
	control->st_mtime = statf->st_mtime;
	control->st_ctime = statf->st_ctime;
	key(control, sizeof(control));
	return 0;
}

#if DEBUGEXEC
void debugexec(char * sh11, int argc, char ** argv)
{
	int i;
	fprintf(stderr, "shll=%s\n", sh11 ? sh11 : "<null>");
	fprintf(stderr, "argc=%d\n", argc);
	if (!argv) {
		fprintf(stderr, "argv=<null>\n");
	} else { 
		for (i = 0; i <= argc ; i++)
			fprintf(stderr, "argv[%d]=%.60s\n", i, argv[i] ? argv[i] : "<null>");
	}
}
#endif /* DEBUGEXEC */

void rmarg(char ** argv, char * arg)
{
	for (; argv && *argv && *argv != arg; argv++);
	for (; argv && *argv; argv++)
		*argv = argv[1];
}

void chkenv_end(void);

int chkenv(int argc)
{
	char buff[512];
	unsigned long mask, m;
	int l, a, c;
	char * string;
	extern char ** environ;

	mask = (unsigned long)getpid();
	stte_0();
	 key(&chkenv, (void*)&chkenv_end - (void*)&chkenv);
	 key(&data, sizeof(data));
	 key(&mask, sizeof(mask));
	arc4(&mask, sizeof(mask));
	sprintf(buff, "x%lx", mask);
	string = getenv(buff);
#if DEBUGEXEC
	fprintf(stderr, "getenv(%s)=%s\n", buff, string ? string : "<null>");
#endif
	l = strlen(buff);
	if (!string) {
		/* 1st */
		sprintf(&buff[l], "=%lu %d", mask, argc);
		putenv(strdup(buff));
		return 0;
	}
	c = sscanf(string, "%lu %d%c", &m, &a, buff);
	if (c == 2 && m == mask) {
		/* 3rd */
		rmarg(environ, &string[-l - 1]);
		return 1 + (argc - a);
	}
	return -1;
}

void chkenv_end(void){}

#if HARDENING

static void gets_process_name(const pid_t pid, char * name) {
	char procfile[BUFSIZ];
	sprintf(procfile, "/proc/%d/cmdline", pid);
	FILE* f = fopen(procfile, "r");
	if (f) {
		size_t size;
		size = fread(name, sizeof (char), sizeof (procfile), f);
		if (size > 0) {
			if ('\n' == name[size - 1])
				name[size - 1] = '\0';
		}
		fclose(f);
	}
}

void hardening() {
    prctl(PR_SET_DUMPABLE, 0);
    prctl(PR_SET_PTRACER, -1);

    int pid = getppid();
    char name[256] = {0};
    gets_process_name(pid, name);

    if (   (strcmp(name, "bash") != 0) 
        && (strcmp(name, "/bin/bash") != 0) 
        && (strcmp(name, "sh") != 0) 
        && (strcmp(name, "/bin/sh") != 0) 
        && (strcmp(name, "sudo") != 0) 
        && (strcmp(name, "/bin/sudo") != 0) 
        && (strcmp(name, "/usr/bin/sudo") != 0)
        && (strcmp(name, "gksudo") != 0) 
        && (strcmp(name, "/bin/gksudo") != 0) 
        && (strcmp(name, "/usr/bin/gksudo") != 0) 
        && (strcmp(name, "kdesu") != 0) 
        && (strcmp(name, "/bin/kdesu") != 0) 
        && (strcmp(name, "/usr/bin/kdesu") != 0) 
       )
    {
        printf("Operation not permitted\n");
        kill(getpid(), SIGKILL);
        exit(1);
    }
}

#endif /* HARDENING */

#if !TRACEABLE

#define _LINUX_SOURCE_COMPAT
#include <sys/ptrace.h>
#include <sys/types.h>
#include <sys/wait.h>
#include <fcntl.h>
#include <signal.h>
#include <stdio.h>
#include <unistd.h>

#if !defined(PT_ATTACHEXC) /* New replacement for PT_ATTACH */
   #if !defined(PTRACE_ATTACH) && defined(PT_ATTACH)
       #define PT_ATTACHEXC	PT_ATTACH
   #elif defined(PTRACE_ATTACH)
       #define PT_ATTACHEXC PTRACE_ATTACH
   #endif
#endif

void untraceable(char * argv0)
{
	char proc[80];
	int pid, mine;

	switch(pid = fork()) {
	case  0:
		pid = getppid();
		/* For problematic SunOS ptrace */
#if defined(__FreeBSD__)
		sprintf(proc, "/proc/%d/mem", (int)pid);
#else
		sprintf(proc, "/proc/%d/as",  (int)pid);
#endif
		close(0);
		mine = !open(proc, O_RDWR|O_EXCL);
		if (!mine && errno != EBUSY)
			mine = !ptrace(PT_ATTACHEXC, pid, 0, 0);
		if (mine) {
			kill(pid, SIGCONT);
		} else {
			perror(argv0);
			kill(pid, SIGKILL);
		}
		_exit(mine);
	case -1:
		break;
	default:
		if (pid == waitpid(pid, 0, 0))
			return;
	}
	perror(argv0);
	_exit(1);
}
#endif /* !TRACEABLE */

char * xsh(int argc, char ** argv)
{
	char * scrpt;
	int ret, i, j;
	char ** varg;
	char * me = argv[0];
	if (me == NULL) { me = getenv("_"); }
	if (me == 0) { fprintf(stderr, "E: neither argv[0] nor $_ works."); exit(1); }

	ret = chkenv(argc);
	stte_0();
	 key(pswd, pswd_z);
	arc4(msg1, msg1_z);
	arc4(date, date_z);
	if (date[0] && (atoll(date)<time(NULL)))
		return msg1;
	arc4(shll, shll_z);
	arc4(inlo, inlo_z);
	arc4(xecc, xecc_z);
	arc4(lsto, lsto_z);
	arc4(tst1, tst1_z);
	 key(tst1, tst1_z);
	arc4(chk1, chk1_z);
	if ((chk1_z != tst1_z) || memcmp(tst1, chk1, tst1_z))
		return tst1;
	arc4(msg2, msg2_z);
	if (ret < 0)
		return msg2;
	varg = (char **)calloc(argc + 10, sizeof(char *));
	if (!varg)
		return 0;
	if (ret) {
		arc4(rlax, rlax_z);
		if (!rlax[0] && key_with_file(shll))
			return shll;
		arc4(opts, opts_z);
#if HARDENING
	    arc4_hardrun(text, text_z);
	    exit(0);
       /* Seccomp Sandboxing - Start */
       seccomp_hardening();
#endif
		arc4(text, text_z);
		arc4(tst2, tst2_z);
		 key(tst2, tst2_z);
		arc4(chk2, chk2_z);
		if ((chk2_z != tst2_z) || memcmp(tst2, chk2, tst2_z))
			return tst2;
		/* Prepend hide_z spaces to script text to hide it. */
		scrpt = malloc(hide_z + text_z);
		if (!scrpt)
			return 0;
		memset(scrpt, (int) ' ', hide_z);
		memcpy(&scrpt[hide_z], text, text_z);
	} else {			/* Reexecute */
		if (*xecc) {
			scrpt = malloc(512);
			if (!scrpt)
				return 0;
			sprintf(scrpt, xecc, me);
		} else {
			scrpt = me;
		}
	}
	j = 0;
#if BUSYBOXON
	varg[j++] = "busybox";
	varg[j++] = "sh";
#else
	varg[j++] = argv[0];		/* My own name at execution */
#endif
	if (ret && *opts)
		varg[j++] = opts;	/* Options on 1st line of code */
	if (*inlo)
		varg[j++] = inlo;	/* Option introducing inline code */
	varg[j++] = scrpt;		/* The script itself */
	if (*lsto)
		varg[j++] = lsto;	/* Option meaning last option */
	i = (ret > 1) ? ret : 0;	/* Args numbering correction */
	while (i < argc)
		varg[j++] = argv[i++];	/* Main run-time arguments */
	varg[j] = 0;			/* NULL terminated array */
#if DEBUGEXEC
	debugexec(shll, j, varg);
#endif
	execvp(shll, varg);
	return shll;
}

int main(int argc, char ** argv)
{
#if SETUID
   setuid(0);
#endif
#if DEBUGEXEC
	debugexec("main", argc, argv);
#endif
#if HARDENING
	hardening();
#endif
#if !TRACEABLE
	untraceable(argv[0]);
#endif
	argv[1] = xsh(argc, argv);
	fprintf(stderr, "%s%s%s: %s\n", argv[0],
		errno ? ": " : "",
		errno ? strerror(errno) : "",
		argv[1] ? argv[1] : "<null>"
	);
	return 1;
}
