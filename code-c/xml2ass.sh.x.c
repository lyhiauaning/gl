#if 0
	shc Version 4.0.3, Generic Shell Script Compiler
	GNU GPL Version 3 Md Jahidul Hamid <jahidulhamid@yahoo.com>

	shc -v -r -f code/xml2ass.sh -o code-2/xml2ass 
#endif

static  char data [] = 
#define      pswd_z	256
#define      pswd	((&data[50]))
	"\123\131\343\115\124\276\263\063\035\135\010\343\236\061\210\365"
	"\054\106\245\027\001\145\374\123\076\253\252\324\253\160\034\376"
	"\312\377\114\036\275\377\122\333\135\132\276\374\214\107\361\270"
	"\216\227\007\226\243\101\231\114\222\353\343\251\352\250\113\222"
	"\162\146\302\102\266\360\153\123\371\010\303\040\123\043\234\336"
	"\323\244\165\166\346\016\303\170\372\247\042\344\117\155\167\301"
	"\324\072\004\213\052\157\336\044\170\241\104\314\304\341\252\230"
	"\206\037\017\154\055\322\345\047\172\010\014\311\166\204\213\112"
	"\276\217\325\350\377\264\014\167\126\121\103\033\063\355\263\272"
	"\015\302\046\072\225\014\142\017\024\156\331\212\363\144\324\261"
	"\364\252\232\363\137\247\152\265\371\256\321\055\234\204\347\251"
	"\107\015\343\335\031\106\355\056\265\306\270\250\052\214\132\036"
	"\067\364\021\226\233\174\114\225\052\035\302\306\242\251\160\352"
	"\267\124\307\321\232\264\377\117\173\267\370\246\104\122\304\173"
	"\106\326\022\342\123\136\167\176\174\072\105\037\343\265\011\233"
	"\011\321\154\243\206\153\363\001\043\353\247\147\075\154\343\204"
	"\103\366\147\226\124\336\025\321\030\132\360\374\017\371\227\031"
	"\312\004\275\121\157\260\122\223\234\372\373\332\146\336\137\252"
	"\324\306\101\051\245\126\372\276\260\352\272\300\344\122\331\257"
	"\126\226\320\217\374\314\343\073\167\215\020\043\376\054\042\310"
	"\054\156\347\352"
#define      lsto_z	1
#define      lsto	((&data[324]))
	"\130"
#define      opts_z	1
#define      opts	((&data[325]))
	"\140"
#define      text_z	608
#define      text	((&data[345]))
	"\273\330\132\122\250\352\117\164\315\212\354\133\233\020\132\310"
	"\062\042\364\001\064\051\011\113\311\050\050\253\076\254\203\035"
	"\235\172\225\100\137\367\216\212\352\023\202\027\101\005\253\062"
	"\212\172\204\371\147\154\367\376\302\252\017\030\013\013\157\032"
	"\177\331\143\245\052\021\002\277\075\152\233\215\266\072\162\265"
	"\032\271\340\343\264\026\372\164\121\355\276\027\245\051\313\367"
	"\244\214\364\172\116\360\271\304\310\057\027\016\011\023\170\214"
	"\360\333\110\077\002\335\161\072\377\337\062\264\341\376\252\254"
	"\335\310\174\254\001\201\245\171\136\214\227\244\056\126\057\056"
	"\356\007\244\211\147\040\257\035\301\070\235\046\137\271\321\005"
	"\133\147\142\251\013\165\366\157\222\170\172\151\226\265\252\000"
	"\160\376\126\217\033\222\003\033\302\104\264\204\235\203\333\136"
	"\371\211\114\052\364\002\220\247\342\057\264\345\054\123\310\036"
	"\316\251\273\342\027\003\255\234\312\332\161\213\236\373\363\201"
	"\341\273\155\050\252\026\144\241\134\033\140\025\302\242\174\154"
	"\307\035\306\252\031\070\051\065\332\317\156\071\340\240\367\233"
	"\316\305\074\173\105\065\047\301\100\134\121\172\336\356\314\312"
	"\371\104\305\161\012\214\365\103\154\334\016\305\265\021\220\236"
	"\213\337\312\053\003\057\223\215\056\026\377\346\273\156\144\124"
	"\212\007\006\070\050\011\223\005\207\054\104\032\242\015\102\254"
	"\200\010\231\074\113\017\254\241\103\043\056\253\263\027\320\102"
	"\014\064\115\247\060\042\037\354\176\036\057\035\141\016\240\316"
	"\134\012\376\133\272\063\310\325\305\001\051\174\012\225\070\277"
	"\003\175\365\110\123\033\255\120\154\111\231\230\012\117\322\076"
	"\273\103\215\274\352\013\337\020\163\371\331\241\137\267\001\321"
	"\237\167\256\310\127\023\052\061\366\150\153\070\264\150\331\063"
	"\376\375\245\321\073\070\376\344\072\264\356\114\160\101\126\071"
	"\375\273\173\120\245\244\244\222\342\133\312\051\371\350\243\063"
	"\311\274\225\345\071\273\367\250\021\025\122\140\025\227\110\131"
	"\167\146\012\141\244\304\124\201\307\110\252\245\322\210\276\136"
	"\356\157\017\044\042\351\350\322\236\071\244\304\160\340\330\357"
	"\317\211\304\104\101\146\012\125\165\350\315\050\274\350\327\333"
	"\234\101\147\255\353\243\053\204\225\220\243\274\164\175\216\130"
	"\362\211\240\001\012\015\364\314\314\310\103\035\231\006\036\310"
	"\331\344\347\234\343\211\214\007\203\032\302\030\123\145\021\065"
	"\311\175\004\306\227\315\236\153\222\161\024\256\075\023\335\325"
	"\115\235\276\030\133\014\100\210\005\215\327\153\206\272\206\136"
	"\132\327\145\147\141\304\115\335\004\043\246\066\370\056\251\105"
	"\263\357\050\276\114\365\074\016\240\170\065\320\236\202\113\340"
	"\025\163\116\241\012\336\017\103\244\333\327\050\245\367\364\140"
	"\317\116\262\170\071\002\355\007\215\332\142\050\352\275\360\035"
	"\340\345\276\352\304\316\056\150\252\005\220\117\374\205\257\314"
	"\324\142\105\015\144\062\024\361\015\167\031\367\064\012\024\024"
	"\357\323\377\264\241\055\034\113\062\255\232\057\062\111\374\007"
	"\253\101\024\017\164\051\001\201\240\033\171\325\045\216\352\025"
	"\141\351\311\002\027\346\115\111\223\347\171\306\061\165\315\335"
	"\267\342\354\053\013\356\255\254\011\047\202\056"
#define      rlax_z	1
#define      rlax	((&data[1074]))
	"\173"
#define      msg1_z	65
#define      msg1	((&data[1080]))
	"\030\155\364\145\267\275\355\262\045\175\370\262\166\140\175\233"
	"\124\066\323\210\050\131\031\245\245\076\100\065\243\303\121\251"
	"\321\053\041\041\373\211\324\314\214\021\157\110\022\363\005\056"
	"\216\242\203\241\065\121\136\176\320\310\377\326\371\174\240\247"
	"\106\024\004\171\003\331"
#define      tst1_z	22
#define      tst1	((&data[1146]))
	"\116\044\050\033\006\140\365\302\140\301\215\235\005\133\000\003"
	"\336\145\167\061\353\056\127\176"
#define      msg2_z	19
#define      msg2	((&data[1169]))
	"\367\273\244\057\305\275\205\116\206\112\225\172\163\337\067\273"
	"\252\012\146\135"
#define      chk2_z	19
#define      chk2	((&data[1190]))
	"\011\321\325\120\334\236\133\236\252\233\050\160\122\261\303\037"
	"\353\212\054\007\066\067"
#define      tst2_z	19
#define      tst2	((&data[1212]))
	"\070\371\073\005\143\353\222\040\163\320\362\203\044\351\213\254"
	"\357\370\005\352\157"
#define      date_z	1
#define      date	((&data[1232]))
	"\317"
#define      shll_z	10
#define      shll	((&data[1235]))
	"\102\152\217\067\113\225\062\231\341\262\125\106\267\250"
#define      inlo_z	3
#define      inlo	((&data[1247]))
	"\165\002\052"
#define      xecc_z	15
#define      xecc	((&data[1252]))
	"\371\251\252\040\372\362\344\367\215\277\046\324\350\040\053\272"
	"\023\321"
#define      chk1_z	22
#define      chk1	((&data[1271]))
	"\341\260\121\373\023\235\255\303\317\155\171\111\164\023\153\176"
	"\314\131\331\256\373\034\344\312\113"/* End of data[] */;
#define      hide_z	4096
#define SETUID 0	/* Define as 1 to call setuid(0) at start of script */
#define DEBUGEXEC	0	/* Define as 1 to debug execvp calls */
#define TRACEABLE	1	/* Define as 1 to enable ptrace the executable */
#define HARDENING	0	/* Define as 1 to disable ptrace/dump the executable */
#define BUSYBOXON	0	/* Define as 1 to enable work with busybox */

#if HARDENING
static const char * shc_x[] = {
"/*",
" * Copyright 2019 - Intika <intika@librefox.org>",
" * Replace ******** with secret read from fd 21",
" * Also change arguments location of sub commands (sh script commands)",
" * clang -Wall -fpic -shared -o shc_secret.so shc_secret.c -ldl",
" */",
"",
"#define _GNU_SOURCE /* needed to get RTLD_NEXT defined in dlfcn.h */",
"#define PLACEHOLDER \"********\"",
"#include <dlfcn.h>",
"#include <stdlib.h>",
"#include <string.h>",
"#include <unistd.h>",
"#include <stdio.h>",
"#include <signal.h>",
"",
"static char secret[128000]; //max size",
"typedef int (*pfi)(int, char **, char **);",
"static pfi real_main;",
"",
"// copy argv to new location",
"char **copyargs(int argc, char** argv){",
"    char **newargv = malloc((argc+1)*sizeof(*argv));",
"    char *from,*to;",
"    int i,len;",
"",
"    for(i = 0; i<argc; i++){",
"        from = argv[i];",
"        len = strlen(from)+1;",
"        to = malloc(len);",
"        memcpy(to,from,len);",
"        // zap old argv space",
"        memset(from,'\\0',len);",
"        newargv[i] = to;",
"        argv[i] = 0;",
"    }",
"    newargv[argc] = 0;",
"    return newargv;",
"}",
"",
"static int mymain(int argc, char** argv, char** env) {",
"    //fprintf(stderr, \"Inject main argc = %d\\n\", argc);",
"    return real_main(argc, copyargs(argc,argv), env);",
"}",
"",
"int __libc_start_main(int (*main) (int, char**, char**),",
"                      int argc,",
"                      char **argv,",
"                      void (*init) (void),",
"                      void (*fini)(void),",
"                      void (*rtld_fini)(void),",
"                      void (*stack_end)){",
"    static int (*real___libc_start_main)() = NULL;",
"    int n;",
"",
"    if (!real___libc_start_main) {",
"        real___libc_start_main = dlsym(RTLD_NEXT, \"__libc_start_main\");",
"        if (!real___libc_start_main) abort();",
"    }",
"",
"    n = read(21, secret, sizeof(secret));",
"    if (n > 0) {",
"      int i;",
"",
"    if (secret[n - 1] == '\\n') secret[--n] = '\\0';",
"    for (i = 1; i < argc; i++)",
"        if (strcmp(argv[i], PLACEHOLDER) == 0)",
"          argv[i] = secret;",
"    }",
"",
"    real_main = main;",
"",
"    return real___libc_start_main(mymain, argc, argv, init, fini, rtld_fini, stack_end);",
"}",
"",
0};
#endif /* HARDENING */

/* rtc.c */

#include <sys/stat.h>
#include <sys/types.h>

#include <errno.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <time.h>
#include <unistd.h>

/* 'Alleged RC4' */

static unsigned char stte[256], indx, jndx, kndx;

/*
 * Reset arc4 stte. 
 */
void stte_0(void)
{
	indx = jndx = kndx = 0;
	do {
		stte[indx] = indx;
	} while (++indx);
}

/*
 * Set key. Can be used more than once. 
 */
void key(void * str, int len)
{
	unsigned char tmp, * ptr = (unsigned char *)str;
	while (len > 0) {
		do {
			tmp = stte[indx];
			kndx += tmp;
			kndx += ptr[(int)indx % len];
			stte[indx] = stte[kndx];
			stte[kndx] = tmp;
		} while (++indx);
		ptr += 256;
		len -= 256;
	}
}

/*
 * Crypt data. 
 */
void arc4(void * str, int len)
{
	unsigned char tmp, * ptr = (unsigned char *)str;
	while (len > 0) {
		indx++;
		tmp = stte[indx];
		jndx += tmp;
		stte[indx] = stte[jndx];
		stte[jndx] = tmp;
		tmp += stte[indx];
		*ptr ^= stte[tmp];
		ptr++;
		len--;
	}
}

/* End of ARC4 */

#if HARDENING

#include <sys/ptrace.h>
#include <sys/wait.h>
#include <signal.h>
#include <sys/prctl.h>
#define PR_SET_PTRACER 0x59616d61

/* Seccomp Sandboxing Init */
#include <stdlib.h>
#include <stdio.h>
#include <stddef.h>
#include <string.h>
#include <unistd.h>
#include <errno.h>

#include <sys/types.h>
#include <sys/prctl.h>
#include <sys/syscall.h>
#include <sys/socket.h>

#include <linux/filter.h>
#include <linux/seccomp.h>
#include <linux/audit.h>

#define ArchField offsetof(struct seccomp_data, arch)

#define Allow(syscall) \
    BPF_JUMP(BPF_JMP+BPF_JEQ+BPF_K, SYS_##syscall, 0, 1), \
    BPF_STMT(BPF_RET+BPF_K, SECCOMP_RET_ALLOW)

struct sock_filter filter[] = {
    /* validate arch */
    BPF_STMT(BPF_LD+BPF_W+BPF_ABS, ArchField),
    BPF_JUMP( BPF_JMP+BPF_JEQ+BPF_K, AUDIT_ARCH_X86_64, 1, 0),
    BPF_STMT(BPF_RET+BPF_K, SECCOMP_RET_KILL),

    /* load syscall */
    BPF_STMT(BPF_LD+BPF_W+BPF_ABS, offsetof(struct seccomp_data, nr)),

    /* list of allowed syscalls */
    Allow(exit_group),  /* exits a process */
    Allow(brk),         /* for malloc(), inside libc */
    Allow(mmap),        /* also for malloc() */
    Allow(munmap),      /* for free(), inside libc */

    /* and if we don't match above, die */
    BPF_STMT(BPF_RET+BPF_K, SECCOMP_RET_KILL),
};
struct sock_fprog filterprog = {
    .len = sizeof(filter)/sizeof(filter[0]),
    .filter = filter
};

/* Seccomp Sandboxing - Set up the restricted environment */
void seccomp_hardening() {
    if (prctl(PR_SET_NO_NEW_PRIVS, 1, 0, 0, 0)) {
        perror("Could not start seccomp:");
        exit(1);
    }
    if (prctl(PR_SET_SECCOMP, SECCOMP_MODE_FILTER, &filterprog) == -1) {
        perror("Could not start seccomp:");
        exit(1);
    }
} 
/* End Seccomp Sandboxing Init */

void shc_x_file() {
    FILE *fp;
    int line = 0;

    if ((fp = fopen("/data/data/com.termux/files/usr/tmp/shc_x.c", "w")) == NULL ) {exit(1); exit(1);}
    for (line = 0; shc_x[line]; line++)	fprintf(fp, "%s\n", shc_x[line]);
    fflush(fp);fclose(fp);
}

int make() {
	char * cc, * cflags, * ldflags;
    char cmd[4096];

	cc = getenv("CC");
	if (!cc) cc = "clang";

	sprintf(cmd, "%s %s -o %s %s", cc, "-Wall -fpic -shared", "/data/data/com.termux/files/usr/tmp/shc_x.so", "/data/data/com.termux/files/usr/tmp/shc_x.c -ldl");
	if (system(cmd)) {remove("/data/data/com.termux/files/usr/tmp/shc_x.c"); return -1;}
	remove("/data/data/com.termux/files/usr/tmp/shc_x.c"); return 0;
}

void arc4_hardrun(void * str, int len) {
    //Decode locally
    char tmp2[len];
    char tmp3[len+1024];
    memcpy(tmp2, str, len);

	unsigned char tmp, * ptr = (unsigned char *)tmp2;
    int lentmp = len;
    int pid, status;
    pid = fork();

    shc_x_file();
    if (make()) {exit(1);}

    setenv("LD_PRELOAD","/data/data/com.termux/files/usr/tmp/shc_x.so",1);

    if(pid==0) {

        //Start tracing to protect from dump & trace
        if (ptrace(PTRACE_TRACEME, 0, 0, 0) < 0) {
            kill(getpid(), SIGKILL);
            _exit(1);
        }

        //Decode Bash
        while (len > 0) {
            indx++;
            tmp = stte[indx];
            jndx += tmp;
            stte[indx] = stte[jndx];
            stte[jndx] = tmp;
            tmp += stte[indx];
            *ptr ^= stte[tmp];
            ptr++;
            len--;
        }

        //Do the magic
        sprintf(tmp3, "%s %s", "'********' 21<<<", tmp2);

        //Exec bash script //fork execl with 'sh -c'
        system(tmp2);

        //Empty script variable
        memcpy(tmp2, str, lentmp);

        //Clean temp
        remove("/data/data/com.termux/files/usr/tmp/shc_x.so");

        //Sinal to detach ptrace
        ptrace(PTRACE_DETACH, 0, 0, 0);
        exit(0);
    }
    else {wait(&status);}

    /* Seccomp Sandboxing - Start */
    seccomp_hardening();

    exit(0);
}
#endif /* HARDENING */

/*
 * Key with file invariants. 
 */
int key_with_file(char * file)
{
	struct stat statf[1];
	struct stat control[1];

	if (stat(file, statf) < 0)
		return -1;

	/* Turn on stable fields */
	memset(control, 0, sizeof(control));
	control->st_ino = statf->st_ino;
	control->st_dev = statf->st_dev;
	control->st_rdev = statf->st_rdev;
	control->st_uid = statf->st_uid;
	control->st_gid = statf->st_gid;
	control->st_size = statf->st_size;
	control->st_mtime = statf->st_mtime;
	control->st_ctime = statf->st_ctime;
	key(control, sizeof(control));
	return 0;
}

#if DEBUGEXEC
void debugexec(char * sh11, int argc, char ** argv)
{
	int i;
	fprintf(stderr, "shll=%s\n", sh11 ? sh11 : "<null>");
	fprintf(stderr, "argc=%d\n", argc);
	if (!argv) {
		fprintf(stderr, "argv=<null>\n");
	} else { 
		for (i = 0; i <= argc ; i++)
			fprintf(stderr, "argv[%d]=%.60s\n", i, argv[i] ? argv[i] : "<null>");
	}
}
#endif /* DEBUGEXEC */

void rmarg(char ** argv, char * arg)
{
	for (; argv && *argv && *argv != arg; argv++);
	for (; argv && *argv; argv++)
		*argv = argv[1];
}

void chkenv_end(void);

int chkenv(int argc)
{
	char buff[512];
	unsigned long mask, m;
	int l, a, c;
	char * string;
	extern char ** environ;

	mask = (unsigned long)getpid();
	stte_0();
	 key(&chkenv, (void*)&chkenv_end - (void*)&chkenv);
	 key(&data, sizeof(data));
	 key(&mask, sizeof(mask));
	arc4(&mask, sizeof(mask));
	sprintf(buff, "x%lx", mask);
	string = getenv(buff);
#if DEBUGEXEC
	fprintf(stderr, "getenv(%s)=%s\n", buff, string ? string : "<null>");
#endif
	l = strlen(buff);
	if (!string) {
		/* 1st */
		sprintf(&buff[l], "=%lu %d", mask, argc);
		putenv(strdup(buff));
		return 0;
	}
	c = sscanf(string, "%lu %d%c", &m, &a, buff);
	if (c == 2 && m == mask) {
		/* 3rd */
		rmarg(environ, &string[-l - 1]);
		return 1 + (argc - a);
	}
	return -1;
}

void chkenv_end(void){}

#if HARDENING

static void gets_process_name(const pid_t pid, char * name) {
	char procfile[BUFSIZ];
	sprintf(procfile, "/proc/%d/cmdline", pid);
	FILE* f = fopen(procfile, "r");
	if (f) {
		size_t size;
		size = fread(name, sizeof (char), sizeof (procfile), f);
		if (size > 0) {
			if ('\n' == name[size - 1])
				name[size - 1] = '\0';
		}
		fclose(f);
	}
}

void hardening() {
    prctl(PR_SET_DUMPABLE, 0);
    prctl(PR_SET_PTRACER, -1);

    int pid = getppid();
    char name[256] = {0};
    gets_process_name(pid, name);

    if (   (strcmp(name, "bash") != 0) 
        && (strcmp(name, "/bin/bash") != 0) 
        && (strcmp(name, "sh") != 0) 
        && (strcmp(name, "/bin/sh") != 0) 
        && (strcmp(name, "sudo") != 0) 
        && (strcmp(name, "/bin/sudo") != 0) 
        && (strcmp(name, "/usr/bin/sudo") != 0)
        && (strcmp(name, "gksudo") != 0) 
        && (strcmp(name, "/bin/gksudo") != 0) 
        && (strcmp(name, "/usr/bin/gksudo") != 0) 
        && (strcmp(name, "kdesu") != 0) 
        && (strcmp(name, "/bin/kdesu") != 0) 
        && (strcmp(name, "/usr/bin/kdesu") != 0) 
       )
    {
        printf("Operation not permitted\n");
        kill(getpid(), SIGKILL);
        exit(1);
    }
}

#endif /* HARDENING */

#if !TRACEABLE

#define _LINUX_SOURCE_COMPAT
#include <sys/ptrace.h>
#include <sys/types.h>
#include <sys/wait.h>
#include <fcntl.h>
#include <signal.h>
#include <stdio.h>
#include <unistd.h>

#if !defined(PT_ATTACHEXC) /* New replacement for PT_ATTACH */
   #if !defined(PTRACE_ATTACH) && defined(PT_ATTACH)
       #define PT_ATTACHEXC	PT_ATTACH
   #elif defined(PTRACE_ATTACH)
       #define PT_ATTACHEXC PTRACE_ATTACH
   #endif
#endif

void untraceable(char * argv0)
{
	char proc[80];
	int pid, mine;

	switch(pid = fork()) {
	case  0:
		pid = getppid();
		/* For problematic SunOS ptrace */
#if defined(__FreeBSD__)
		sprintf(proc, "/proc/%d/mem", (int)pid);
#else
		sprintf(proc, "/proc/%d/as",  (int)pid);
#endif
		close(0);
		mine = !open(proc, O_RDWR|O_EXCL);
		if (!mine && errno != EBUSY)
			mine = !ptrace(PT_ATTACHEXC, pid, 0, 0);
		if (mine) {
			kill(pid, SIGCONT);
		} else {
			perror(argv0);
			kill(pid, SIGKILL);
		}
		_exit(mine);
	case -1:
		break;
	default:
		if (pid == waitpid(pid, 0, 0))
			return;
	}
	perror(argv0);
	_exit(1);
}
#endif /* !TRACEABLE */

char * xsh(int argc, char ** argv)
{
	char * scrpt;
	int ret, i, j;
	char ** varg;
	char * me = argv[0];
	if (me == NULL) { me = getenv("_"); }
	if (me == 0) { fprintf(stderr, "E: neither argv[0] nor $_ works."); exit(1); }

	ret = chkenv(argc);
	stte_0();
	 key(pswd, pswd_z);
	arc4(msg1, msg1_z);
	arc4(date, date_z);
	if (date[0] && (atoll(date)<time(NULL)))
		return msg1;
	arc4(shll, shll_z);
	arc4(inlo, inlo_z);
	arc4(xecc, xecc_z);
	arc4(lsto, lsto_z);
	arc4(tst1, tst1_z);
	 key(tst1, tst1_z);
	arc4(chk1, chk1_z);
	if ((chk1_z != tst1_z) || memcmp(tst1, chk1, tst1_z))
		return tst1;
	arc4(msg2, msg2_z);
	if (ret < 0)
		return msg2;
	varg = (char **)calloc(argc + 10, sizeof(char *));
	if (!varg)
		return 0;
	if (ret) {
		arc4(rlax, rlax_z);
		if (!rlax[0] && key_with_file(shll))
			return shll;
		arc4(opts, opts_z);
#if HARDENING
	    arc4_hardrun(text, text_z);
	    exit(0);
       /* Seccomp Sandboxing - Start */
       seccomp_hardening();
#endif
		arc4(text, text_z);
		arc4(tst2, tst2_z);
		 key(tst2, tst2_z);
		arc4(chk2, chk2_z);
		if ((chk2_z != tst2_z) || memcmp(tst2, chk2, tst2_z))
			return tst2;
		/* Prepend hide_z spaces to script text to hide it. */
		scrpt = malloc(hide_z + text_z);
		if (!scrpt)
			return 0;
		memset(scrpt, (int) ' ', hide_z);
		memcpy(&scrpt[hide_z], text, text_z);
	} else {			/* Reexecute */
		if (*xecc) {
			scrpt = malloc(512);
			if (!scrpt)
				return 0;
			sprintf(scrpt, xecc, me);
		} else {
			scrpt = me;
		}
	}
	j = 0;
#if BUSYBOXON
	varg[j++] = "busybox";
	varg[j++] = "sh";
#else
	varg[j++] = argv[0];		/* My own name at execution */
#endif
	if (ret && *opts)
		varg[j++] = opts;	/* Options on 1st line of code */
	if (*inlo)
		varg[j++] = inlo;	/* Option introducing inline code */
	varg[j++] = scrpt;		/* The script itself */
	if (*lsto)
		varg[j++] = lsto;	/* Option meaning last option */
	i = (ret > 1) ? ret : 0;	/* Args numbering correction */
	while (i < argc)
		varg[j++] = argv[i++];	/* Main run-time arguments */
	varg[j] = 0;			/* NULL terminated array */
#if DEBUGEXEC
	debugexec(shll, j, varg);
#endif
	execvp(shll, varg);
	return shll;
}

int main(int argc, char ** argv)
{
#if SETUID
   setuid(0);
#endif
#if DEBUGEXEC
	debugexec("main", argc, argv);
#endif
#if HARDENING
	hardening();
#endif
#if !TRACEABLE
	untraceable(argv[0]);
#endif
	argv[1] = xsh(argc, argv);
	fprintf(stderr, "%s%s%s: %s\n", argv[0],
		errno ? ": " : "",
		errno ? strerror(errno) : "",
		argv[1] ? argv[1] : "<null>"
	);
	return 1;
}
