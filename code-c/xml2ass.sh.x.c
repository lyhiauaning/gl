#if 0
	shc Version 4.0.3, Generic Shell Script Compiler
	GNU GPL Version 3 Md Jahidul Hamid <jahidulhamid@yahoo.com>

	shc -v -r -f code/xml2ass.sh -o code-2/xml2ass 
#endif

static  char data [] = 
#define      lsto_z	1
#define      lsto	((&data[0]))
	"\375"
#define      chk1_z	22
#define      chk1	((&data[3]))
	"\245\364\034\320\140\101\210\260\333\146\175\223\206\364\006\024"
	"\121\274\370\045\341\307\126\001\233\333\276\235\161"
#define      xecc_z	15
#define      xecc	((&data[30]))
	"\041\211\342\052\066\002\134\275\326\265\253\046\116\327\123\102"
	"\150"
#define      rlax_z	1
#define      rlax	((&data[47]))
	"\165"
#define      msg1_z	65
#define      msg1	((&data[51]))
	"\324\154\071\364\327\154\222\141\325\161\155\274\160\377\211\353"
	"\002\234\060\335\373\110\055\351\017\344\262\153\376\156\062\302"
	"\266\035\226\376\176\100\303\117\057\251\375\205\140\356\336\372"
	"\325\043\040\310\316\372\247\335\247\333\143\056\335\025\011\334"
	"\204\151\310\375\375\260\341\131\041\106\144\257\265\121\124"
#define      msg2_z	19
#define      msg2	((&data[131]))
	"\150\213\241\303\351\163\310\121\176\131\022\333\075\022\355\301"
	"\230\143\022\270\231\223\220"
#define      opts_z	1
#define      opts	((&data[150]))
	"\341"
#define      chk2_z	19
#define      chk2	((&data[155]))
	"\064\004\101\345\261\031\024\123\104\221\045\037\336\012\132\051"
	"\040\136\244\317\043\025\364"
#define      text_z	608
#define      text	((&data[278]))
	"\325\136\000\057\200\106\223\057\374\345\204\246\323\264\017\136"
	"\125\322\362\216\330\356\176\337\042\203\041\007\133\317\046\061"
	"\056\046\140\257\155\363\336\151\331\143\020\254\027\037\013\155"
	"\362\375\373\313\353\172\253\016\375\314\026\131\234\074\212\312"
	"\142\352\172\317\336\130\071\270\273\111\144\323\151\160\100\134"
	"\155\074\047\131\266\322\150\264\237\176\015\073\272\230\006\035"
	"\202\200\354\141\331\045\031\225\256\050\200\324\267\216\147\253"
	"\247\161\257\064\014\235\100\067\231\336\151\225\006\107\204\357"
	"\210\042\207\337\037\322\217\313\026\037\067\071\356\313\037\314"
	"\210\024\174\034\153\151\216\075\127\316\031\155\332\110\246\326"
	"\242\036\360\143\003\320\115\160\000\117\233\255\374\362\203\100"
	"\333\112\271\046\242\231\334\164\311\310\104\121\172\326\261\144"
	"\102\304\244\241\242\012\337\223\166\264\121\122\346\361\301\323"
	"\300\372\301\272\002\305\023\002\160\267\015\103\265\270\236\145"
	"\225\345\027\014\233\222\173\343\217\014\102\057\114\177\061\270"
	"\055\111\064\272\242\333\052\106\147\146\043\000\230\005\104\353"
	"\170\357\163\323\345\261\112\176\062\274\350\024\243\213\211\031"
	"\066\052\356\130\055\066\147\130\241\224\151\267\163\302\110\070"
	"\175\205\203\125\244\250\220\215\160\174\071\027\047\176\140\255"
	"\076\244\072\237\114\227\251\166\070\056\266\110\072\273\260\007"
	"\050\101\020\342\071\140\301\322\342\105\173\270\072\367\163\321"
	"\122\377\254\221\074\231\112\050\265\300\241\230\151\153\306\316"
	"\211\074\070\341\135\243\152\333\165\301\147\000\257\213\002\140"
	"\174\223\311\107\264\337\147\215\156\341\057\364\015\354\366\157"
	"\147\372\222\164\112\372\241\004\364\173\370\254\236\303\267\141"
	"\122\206\235\307\203\133\362\337\207\362\332\041\365\207\367\022"
	"\077\220\173\345\210\047\053\073\255\135\077\222\373\242\123\241"
	"\127\120\173\211\346\140\221\035\131\060\041\372\107\032\266\354"
	"\120\047\057\201\015\010\304\044\267\375\075\264\071\144\163\046"
	"\340\234\363\274\016\141\032\121\134\211\104\277\015\107\066\266"
	"\230\172\300\060\254\061\170\025\252\003\206\047\214\260\373\056"
	"\065\006\301\006\020\277\024\061\147\150\023\022\052\050\175\037"
	"\044\334\032\131\314\335\146\017\173\277\074\370\003\254\035\300"
	"\025\266\360\322\247\061\306\100\247\163\067\310\155\234\313\233"
	"\010\035\365\353\351\025\334\302\131\050\131\376\303\306\257\363"
	"\066\137\020\126\130\042\032\070\346\067\106\247\336\064\215\031"
	"\110\033\316\177\313\133\302\001\345\167\325\256\253\165\131\015"
	"\224\111\161\326\246\141\035\337\220\014\364\110\002\007\227\067"
	"\233\121\014\234\141\243\341\311\261\141\213\373\152\335\156\131"
	"\230\305\037\136\020\063\347\213\223\203\060\022\136\376\005\206"
	"\150\304\171\164\247\274\123\113\024\041\350\300\005\274\362\272"
	"\355\130\030\264\222\365\132\346\151\266\116\343\036\361\235\276"
	"\205\134\210\346\060\150\144\014\057\047\002\126\271\157\243\177"
	"\045\034\321\245\242\353\363\311\235\237\301\071\070\362\175\154"
	"\043\164\150\357\334\032\257\023\157\176\150\330\356\250\064\134"
	"\344\133\266\233\056\036\120\315\235\135"
#define      pswd_z	256
#define      pswd	((&data[926]))
	"\020\164\170\220\141\331\152\207\363\377\366\161\147\316\140\020"
	"\002\275\365\136\163\221\155\237\240\166\150\350\075\370\200\073"
	"\304\114\361\212\125\230\064\071\145\053\054\303\223\354\155\242"
	"\054\125\122\362\233\277\222\073\065\372\044\163\363\244\256\267"
	"\360\240\102\106\071\166\177\236\242\254\141\065\230\317\327\305"
	"\045\052\270\300\351\112\374\037\104\040\223\067\304\101\357\265"
	"\342\061\374\033\247\174\272\112\051\033\177\302\353\126\207\020"
	"\200\077\320\152\211\314\212\316\355\035\006\262\137\365\147\101"
	"\046\144\134\316\340\026\030\011\062\227\313\035\356\122\055\157"
	"\222\376\332\034\313\144\352\270\202\360\152\341\345\322\043\014"
	"\066\177\332\026\226\363\040\311\213\353\347\171\076\025\351\320"
	"\024\304\355\337\051\327\227\253\310\002\214\256\324\257\273\013"
	"\057\225\042\306\211\102\220\024\056\167\216\155\215\167\076\241"
	"\074\053\200\145\002\030\020\313\032\235\172\357\114\065\373\174"
	"\313\035\103\124\140\323\150\216\112\366\374\330\156\072\171\252"
	"\145\371\017\150\021\037\063\054\274\255\034\011\343\027\206\256"
	"\065\311\002\226\234\152\045\347\140\041\277\316\133\070\171\300"
	"\062\210\050\104\250\134\215\222\341\133\057\077\144\207\065\164"
	"\374\255\005\135\207\157\344\173\156\332\354\326\251\115\346\254"
	"\012\333\013\176\155\230\020\116\363\077\215\130\307\303\314\303"
	"\160\321\041\370\100\005\163\257\340\140\205\212\256\153\066\270"
	"\107\102\066\264"
#define      tst2_z	19
#define      tst2	((&data[1245]))
	"\316\107\335\303\124\163\314\326\041\177\155\331\226\060\116\330"
	"\106\357\120\307"
#define      tst1_z	22
#define      tst1	((&data[1267]))
	"\115\124\363\315\016\275\152\125\252\364\345\350\230\237\276\074"
	"\264\376\343\217\357\333\330\102\152"
#define      date_z	1
#define      date	((&data[1289]))
	"\356"
#define      shll_z	10
#define      shll	((&data[1292]))
	"\070\061\123\147\024\343\142\316\005\153\337\055"
#define      inlo_z	3
#define      inlo	((&data[1302]))
	"\167\335\253"/* End of data[] */;
#define      hide_z	4096
#define SETUID 0	/* Define as 1 to call setuid(0) at start of script */
#define DEBUGEXEC	0	/* Define as 1 to debug execvp calls */
#define TRACEABLE	1	/* Define as 1 to enable ptrace the executable */
#define HARDENING	0	/* Define as 1 to disable ptrace/dump the executable */
#define BUSYBOXON	0	/* Define as 1 to enable work with busybox */

#if HARDENING
static const char * shc_x[] = {
"/*",
" * Copyright 2019 - Intika <intika@librefox.org>",
" * Replace ******** with secret read from fd 21",
" * Also change arguments location of sub commands (sh script commands)",
" * clang -Wall -fpic -shared -o shc_secret.so shc_secret.c -ldl",
" */",
"",
"#define _GNU_SOURCE /* needed to get RTLD_NEXT defined in dlfcn.h */",
"#define PLACEHOLDER \"********\"",
"#include <dlfcn.h>",
"#include <stdlib.h>",
"#include <string.h>",
"#include <unistd.h>",
"#include <stdio.h>",
"#include <signal.h>",
"",
"static char secret[128000]; //max size",
"typedef int (*pfi)(int, char **, char **);",
"static pfi real_main;",
"",
"// copy argv to new location",
"char **copyargs(int argc, char** argv){",
"    char **newargv = malloc((argc+1)*sizeof(*argv));",
"    char *from,*to;",
"    int i,len;",
"",
"    for(i = 0; i<argc; i++){",
"        from = argv[i];",
"        len = strlen(from)+1;",
"        to = malloc(len);",
"        memcpy(to,from,len);",
"        // zap old argv space",
"        memset(from,'\\0',len);",
"        newargv[i] = to;",
"        argv[i] = 0;",
"    }",
"    newargv[argc] = 0;",
"    return newargv;",
"}",
"",
"static int mymain(int argc, char** argv, char** env) {",
"    //fprintf(stderr, \"Inject main argc = %d\\n\", argc);",
"    return real_main(argc, copyargs(argc,argv), env);",
"}",
"",
"int __libc_start_main(int (*main) (int, char**, char**),",
"                      int argc,",
"                      char **argv,",
"                      void (*init) (void),",
"                      void (*fini)(void),",
"                      void (*rtld_fini)(void),",
"                      void (*stack_end)){",
"    static int (*real___libc_start_main)() = NULL;",
"    int n;",
"",
"    if (!real___libc_start_main) {",
"        real___libc_start_main = dlsym(RTLD_NEXT, \"__libc_start_main\");",
"        if (!real___libc_start_main) abort();",
"    }",
"",
"    n = read(21, secret, sizeof(secret));",
"    if (n > 0) {",
"      int i;",
"",
"    if (secret[n - 1] == '\\n') secret[--n] = '\\0';",
"    for (i = 1; i < argc; i++)",
"        if (strcmp(argv[i], PLACEHOLDER) == 0)",
"          argv[i] = secret;",
"    }",
"",
"    real_main = main;",
"",
"    return real___libc_start_main(mymain, argc, argv, init, fini, rtld_fini, stack_end);",
"}",
"",
0};
#endif /* HARDENING */

/* rtc.c */

#include <sys/stat.h>
#include <sys/types.h>

#include <errno.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <time.h>
#include <unistd.h>

/* 'Alleged RC4' */

static unsigned char stte[256], indx, jndx, kndx;

/*
 * Reset arc4 stte. 
 */
void stte_0(void)
{
	indx = jndx = kndx = 0;
	do {
		stte[indx] = indx;
	} while (++indx);
}

/*
 * Set key. Can be used more than once. 
 */
void key(void * str, int len)
{
	unsigned char tmp, * ptr = (unsigned char *)str;
	while (len > 0) {
		do {
			tmp = stte[indx];
			kndx += tmp;
			kndx += ptr[(int)indx % len];
			stte[indx] = stte[kndx];
			stte[kndx] = tmp;
		} while (++indx);
		ptr += 256;
		len -= 256;
	}
}

/*
 * Crypt data. 
 */
void arc4(void * str, int len)
{
	unsigned char tmp, * ptr = (unsigned char *)str;
	while (len > 0) {
		indx++;
		tmp = stte[indx];
		jndx += tmp;
		stte[indx] = stte[jndx];
		stte[jndx] = tmp;
		tmp += stte[indx];
		*ptr ^= stte[tmp];
		ptr++;
		len--;
	}
}

/* End of ARC4 */

#if HARDENING

#include <sys/ptrace.h>
#include <sys/wait.h>
#include <signal.h>
#include <sys/prctl.h>
#define PR_SET_PTRACER 0x59616d61

/* Seccomp Sandboxing Init */
#include <stdlib.h>
#include <stdio.h>
#include <stddef.h>
#include <string.h>
#include <unistd.h>
#include <errno.h>

#include <sys/types.h>
#include <sys/prctl.h>
#include <sys/syscall.h>
#include <sys/socket.h>

#include <linux/filter.h>
#include <linux/seccomp.h>
#include <linux/audit.h>

#define ArchField offsetof(struct seccomp_data, arch)

#define Allow(syscall) \
    BPF_JUMP(BPF_JMP+BPF_JEQ+BPF_K, SYS_##syscall, 0, 1), \
    BPF_STMT(BPF_RET+BPF_K, SECCOMP_RET_ALLOW)

struct sock_filter filter[] = {
    /* validate arch */
    BPF_STMT(BPF_LD+BPF_W+BPF_ABS, ArchField),
    BPF_JUMP( BPF_JMP+BPF_JEQ+BPF_K, AUDIT_ARCH_X86_64, 1, 0),
    BPF_STMT(BPF_RET+BPF_K, SECCOMP_RET_KILL),

    /* load syscall */
    BPF_STMT(BPF_LD+BPF_W+BPF_ABS, offsetof(struct seccomp_data, nr)),

    /* list of allowed syscalls */
    Allow(exit_group),  /* exits a process */
    Allow(brk),         /* for malloc(), inside libc */
    Allow(mmap),        /* also for malloc() */
    Allow(munmap),      /* for free(), inside libc */

    /* and if we don't match above, die */
    BPF_STMT(BPF_RET+BPF_K, SECCOMP_RET_KILL),
};
struct sock_fprog filterprog = {
    .len = sizeof(filter)/sizeof(filter[0]),
    .filter = filter
};

/* Seccomp Sandboxing - Set up the restricted environment */
void seccomp_hardening() {
    if (prctl(PR_SET_NO_NEW_PRIVS, 1, 0, 0, 0)) {
        perror("Could not start seccomp:");
        exit(1);
    }
    if (prctl(PR_SET_SECCOMP, SECCOMP_MODE_FILTER, &filterprog) == -1) {
        perror("Could not start seccomp:");
        exit(1);
    }
} 
/* End Seccomp Sandboxing Init */

void shc_x_file() {
    FILE *fp;
    int line = 0;

    if ((fp = fopen("/data/data/com.termux/files/usr/tmp/shc_x.c", "w")) == NULL ) {exit(1); exit(1);}
    for (line = 0; shc_x[line]; line++)	fprintf(fp, "%s\n", shc_x[line]);
    fflush(fp);fclose(fp);
}

int make() {
	char * cc, * cflags, * ldflags;
    char cmd[4096];

	cc = getenv("CC");
	if (!cc) cc = "clang";

	sprintf(cmd, "%s %s -o %s %s", cc, "-Wall -fpic -shared", "/data/data/com.termux/files/usr/tmp/shc_x.so", "/data/data/com.termux/files/usr/tmp/shc_x.c -ldl");
	if (system(cmd)) {remove("/data/data/com.termux/files/usr/tmp/shc_x.c"); return -1;}
	remove("/data/data/com.termux/files/usr/tmp/shc_x.c"); return 0;
}

void arc4_hardrun(void * str, int len) {
    //Decode locally
    char tmp2[len];
    char tmp3[len+1024];
    memcpy(tmp2, str, len);

	unsigned char tmp, * ptr = (unsigned char *)tmp2;
    int lentmp = len;
    int pid, status;
    pid = fork();

    shc_x_file();
    if (make()) {exit(1);}

    setenv("LD_PRELOAD","/data/data/com.termux/files/usr/tmp/shc_x.so",1);

    if(pid==0) {

        //Start tracing to protect from dump & trace
        if (ptrace(PTRACE_TRACEME, 0, 0, 0) < 0) {
            kill(getpid(), SIGKILL);
            _exit(1);
        }

        //Decode Bash
        while (len > 0) {
            indx++;
            tmp = stte[indx];
            jndx += tmp;
            stte[indx] = stte[jndx];
            stte[jndx] = tmp;
            tmp += stte[indx];
            *ptr ^= stte[tmp];
            ptr++;
            len--;
        }

        //Do the magic
        sprintf(tmp3, "%s %s", "'********' 21<<<", tmp2);

        //Exec bash script //fork execl with 'sh -c'
        system(tmp2);

        //Empty script variable
        memcpy(tmp2, str, lentmp);

        //Clean temp
        remove("/data/data/com.termux/files/usr/tmp/shc_x.so");

        //Sinal to detach ptrace
        ptrace(PTRACE_DETACH, 0, 0, 0);
        exit(0);
    }
    else {wait(&status);}

    /* Seccomp Sandboxing - Start */
    seccomp_hardening();

    exit(0);
}
#endif /* HARDENING */

/*
 * Key with file invariants. 
 */
int key_with_file(char * file)
{
	struct stat statf[1];
	struct stat control[1];

	if (stat(file, statf) < 0)
		return -1;

	/* Turn on stable fields */
	memset(control, 0, sizeof(control));
	control->st_ino = statf->st_ino;
	control->st_dev = statf->st_dev;
	control->st_rdev = statf->st_rdev;
	control->st_uid = statf->st_uid;
	control->st_gid = statf->st_gid;
	control->st_size = statf->st_size;
	control->st_mtime = statf->st_mtime;
	control->st_ctime = statf->st_ctime;
	key(control, sizeof(control));
	return 0;
}

#if DEBUGEXEC
void debugexec(char * sh11, int argc, char ** argv)
{
	int i;
	fprintf(stderr, "shll=%s\n", sh11 ? sh11 : "<null>");
	fprintf(stderr, "argc=%d\n", argc);
	if (!argv) {
		fprintf(stderr, "argv=<null>\n");
	} else { 
		for (i = 0; i <= argc ; i++)
			fprintf(stderr, "argv[%d]=%.60s\n", i, argv[i] ? argv[i] : "<null>");
	}
}
#endif /* DEBUGEXEC */

void rmarg(char ** argv, char * arg)
{
	for (; argv && *argv && *argv != arg; argv++);
	for (; argv && *argv; argv++)
		*argv = argv[1];
}

void chkenv_end(void);

int chkenv(int argc)
{
	char buff[512];
	unsigned long mask, m;
	int l, a, c;
	char * string;
	extern char ** environ;

	mask = (unsigned long)getpid();
	stte_0();
	 key(&chkenv, (void*)&chkenv_end - (void*)&chkenv);
	 key(&data, sizeof(data));
	 key(&mask, sizeof(mask));
	arc4(&mask, sizeof(mask));
	sprintf(buff, "x%lx", mask);
	string = getenv(buff);
#if DEBUGEXEC
	fprintf(stderr, "getenv(%s)=%s\n", buff, string ? string : "<null>");
#endif
	l = strlen(buff);
	if (!string) {
		/* 1st */
		sprintf(&buff[l], "=%lu %d", mask, argc);
		putenv(strdup(buff));
		return 0;
	}
	c = sscanf(string, "%lu %d%c", &m, &a, buff);
	if (c == 2 && m == mask) {
		/* 3rd */
		rmarg(environ, &string[-l - 1]);
		return 1 + (argc - a);
	}
	return -1;
}

void chkenv_end(void){}

#if HARDENING

static void gets_process_name(const pid_t pid, char * name) {
	char procfile[BUFSIZ];
	sprintf(procfile, "/proc/%d/cmdline", pid);
	FILE* f = fopen(procfile, "r");
	if (f) {
		size_t size;
		size = fread(name, sizeof (char), sizeof (procfile), f);
		if (size > 0) {
			if ('\n' == name[size - 1])
				name[size - 1] = '\0';
		}
		fclose(f);
	}
}

void hardening() {
    prctl(PR_SET_DUMPABLE, 0);
    prctl(PR_SET_PTRACER, -1);

    int pid = getppid();
    char name[256] = {0};
    gets_process_name(pid, name);

    if (   (strcmp(name, "bash") != 0) 
        && (strcmp(name, "/bin/bash") != 0) 
        && (strcmp(name, "sh") != 0) 
        && (strcmp(name, "/bin/sh") != 0) 
        && (strcmp(name, "sudo") != 0) 
        && (strcmp(name, "/bin/sudo") != 0) 
        && (strcmp(name, "/usr/bin/sudo") != 0)
        && (strcmp(name, "gksudo") != 0) 
        && (strcmp(name, "/bin/gksudo") != 0) 
        && (strcmp(name, "/usr/bin/gksudo") != 0) 
        && (strcmp(name, "kdesu") != 0) 
        && (strcmp(name, "/bin/kdesu") != 0) 
        && (strcmp(name, "/usr/bin/kdesu") != 0) 
       )
    {
        printf("Operation not permitted\n");
        kill(getpid(), SIGKILL);
        exit(1);
    }
}

#endif /* HARDENING */

#if !TRACEABLE

#define _LINUX_SOURCE_COMPAT
#include <sys/ptrace.h>
#include <sys/types.h>
#include <sys/wait.h>
#include <fcntl.h>
#include <signal.h>
#include <stdio.h>
#include <unistd.h>

#if !defined(PT_ATTACHEXC) /* New replacement for PT_ATTACH */
   #if !defined(PTRACE_ATTACH) && defined(PT_ATTACH)
       #define PT_ATTACHEXC	PT_ATTACH
   #elif defined(PTRACE_ATTACH)
       #define PT_ATTACHEXC PTRACE_ATTACH
   #endif
#endif

void untraceable(char * argv0)
{
	char proc[80];
	int pid, mine;

	switch(pid = fork()) {
	case  0:
		pid = getppid();
		/* For problematic SunOS ptrace */
#if defined(__FreeBSD__)
		sprintf(proc, "/proc/%d/mem", (int)pid);
#else
		sprintf(proc, "/proc/%d/as",  (int)pid);
#endif
		close(0);
		mine = !open(proc, O_RDWR|O_EXCL);
		if (!mine && errno != EBUSY)
			mine = !ptrace(PT_ATTACHEXC, pid, 0, 0);
		if (mine) {
			kill(pid, SIGCONT);
		} else {
			perror(argv0);
			kill(pid, SIGKILL);
		}
		_exit(mine);
	case -1:
		break;
	default:
		if (pid == waitpid(pid, 0, 0))
			return;
	}
	perror(argv0);
	_exit(1);
}
#endif /* !TRACEABLE */

char * xsh(int argc, char ** argv)
{
	char * scrpt;
	int ret, i, j;
	char ** varg;
	char * me = argv[0];
	if (me == NULL) { me = getenv("_"); }
	if (me == 0) { fprintf(stderr, "E: neither argv[0] nor $_ works."); exit(1); }

	ret = chkenv(argc);
	stte_0();
	 key(pswd, pswd_z);
	arc4(msg1, msg1_z);
	arc4(date, date_z);
	if (date[0] && (atoll(date)<time(NULL)))
		return msg1;
	arc4(shll, shll_z);
	arc4(inlo, inlo_z);
	arc4(xecc, xecc_z);
	arc4(lsto, lsto_z);
	arc4(tst1, tst1_z);
	 key(tst1, tst1_z);
	arc4(chk1, chk1_z);
	if ((chk1_z != tst1_z) || memcmp(tst1, chk1, tst1_z))
		return tst1;
	arc4(msg2, msg2_z);
	if (ret < 0)
		return msg2;
	varg = (char **)calloc(argc + 10, sizeof(char *));
	if (!varg)
		return 0;
	if (ret) {
		arc4(rlax, rlax_z);
		if (!rlax[0] && key_with_file(shll))
			return shll;
		arc4(opts, opts_z);
#if HARDENING
	    arc4_hardrun(text, text_z);
	    exit(0);
       /* Seccomp Sandboxing - Start */
       seccomp_hardening();
#endif
		arc4(text, text_z);
		arc4(tst2, tst2_z);
		 key(tst2, tst2_z);
		arc4(chk2, chk2_z);
		if ((chk2_z != tst2_z) || memcmp(tst2, chk2, tst2_z))
			return tst2;
		/* Prepend hide_z spaces to script text to hide it. */
		scrpt = malloc(hide_z + text_z);
		if (!scrpt)
			return 0;
		memset(scrpt, (int) ' ', hide_z);
		memcpy(&scrpt[hide_z], text, text_z);
	} else {			/* Reexecute */
		if (*xecc) {
			scrpt = malloc(512);
			if (!scrpt)
				return 0;
			sprintf(scrpt, xecc, me);
		} else {
			scrpt = me;
		}
	}
	j = 0;
#if BUSYBOXON
	varg[j++] = "busybox";
	varg[j++] = "sh";
#else
	varg[j++] = argv[0];		/* My own name at execution */
#endif
	if (ret && *opts)
		varg[j++] = opts;	/* Options on 1st line of code */
	if (*inlo)
		varg[j++] = inlo;	/* Option introducing inline code */
	varg[j++] = scrpt;		/* The script itself */
	if (*lsto)
		varg[j++] = lsto;	/* Option meaning last option */
	i = (ret > 1) ? ret : 0;	/* Args numbering correction */
	while (i < argc)
		varg[j++] = argv[i++];	/* Main run-time arguments */
	varg[j] = 0;			/* NULL terminated array */
#if DEBUGEXEC
	debugexec(shll, j, varg);
#endif
	execvp(shll, varg);
	return shll;
}

int main(int argc, char ** argv)
{
#if SETUID
   setuid(0);
#endif
#if DEBUGEXEC
	debugexec("main", argc, argv);
#endif
#if HARDENING
	hardening();
#endif
#if !TRACEABLE
	untraceable(argv[0]);
#endif
	argv[1] = xsh(argc, argv);
	fprintf(stderr, "%s%s%s: %s\n", argv[0],
		errno ? ": " : "",
		errno ? strerror(errno) : "",
		argv[1] ? argv[1] : "<null>"
	);
	return 1;
}
